"""Windows semantic observation (Milestone 9, slice 1): inventory and read-only UIA.

Observation only. Nothing in this package can change a desktop UI: it cannot focus,
invoke, type into, select, scroll, click, move, resize, launch or close anything. The
source scan in `tests/desktop_source_scan.py` enforces that on every test run.
"""
