"""Evaluation infrastructure.

Nothing under `evals/` is production Lumi code. It exists so browser execution
and failure behaviour can be measured against sites whose state is fully known,
and it is never imported by `app/`.
"""
